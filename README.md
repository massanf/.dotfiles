## Prerequisites
- stow
- zsh
  - Framework: oh-my-zsh
  - Theme: powerlevel10k
- tmux
  - Theme: iceberg_minimal (installed)

## stow
Link `~/.vimrc` to `~/dotfiles/zsh/.vimrc`, etc.
```
cd ~/dotfiles
stow [zsh|tmux|vim|p10k]
```

